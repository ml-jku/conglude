_target_: pytorch_lightning.Trainer
check_val_every_n_epoch: 1
log_every_n_steps: 10
accelerator: auto
strategy: auto
devices: auto
max_epochs: 500
num_sanity_val_steps: 0
deterministic: false
gradient_clip_val: 1.0  # Maximum L2 norm of gradients
gradient_clip_algorithm: norm